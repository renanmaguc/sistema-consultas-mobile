import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: "#f5f5f5",
    },
    conteudo: {
        padding: 20,
        paddingBottom: 40,
    },
    secao: {
        backgroundColor: "#fff",
        padding: 20,
        borderRadius: 8,
        marginBottom: 16,
    },
    titulo: {
        fontSize: 18,
        fontWeight: "bold",
        color: "#333",
        marginBottom: 8,
    },
    texto: {
        fontSize: 14,
        color: "#666",
        marginBottom: 12,
    },
    input: {
        backgroundColor: "#f5f5f5",
        borderWidth: 1,
        borderColor: "#ddd",
        borderRadius: 8,
        padding: 12,
        marginBottom: 10,
        fontSize: 16,
    },
});